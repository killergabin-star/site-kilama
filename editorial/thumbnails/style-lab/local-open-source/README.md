# Local Open Source Bench

This folder is reserved for legitimate local image-generation tests.

Recommended target:

- install ComfyUI or export from Draw Things;
- keep raw outputs in `outputs/raw/`;
- keep selected candidates in `outputs/selected/`;
- record prompts and model settings in `runs/`.

Do not commit huge model weights here.

Suggested external model cache:

`/Volumes/T7 sharing/ai-image-lab/models/`
